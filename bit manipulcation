arr=list(map(int,input().split()))
n=len(arr)
total_subsets=(1<<n)
ans=[]
for num in range(total_subsets):
    curr_subsets=[]
    for i in range(n):
        if(num&(1<<i)!=0):
            curr_subsets.append(arr[i])
    ans.append(curr_subsets)
print(ans)
